LaDaube server module
---------------------

The web front-end is implemented in Java/Groovy/Stripes and relies on a mongo database.

to compile and build the war :
  mvn clean install

TODO document mongo setup etc